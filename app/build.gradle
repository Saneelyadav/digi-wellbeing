plugins {
    // I added the version number below. This was the missing key!
    id 'com.android.application' version '8.1.0'
}

android {
    namespace 'com.example.shortsblocker'
    compileSdk 33

    defaultConfig {
        applicationId "com.example.shortsblocker"
        minSdk 24
        targetSdk 33
        versionCode 1
        versionName "1.0"
    }
}
